//! Notación
// ; --> delimitar el final de una linea
const b = 4;
b + 4;

// . --> Se usa en los objectos para acceder a los atributos
//todo movil.anchura

// [] --> Listas, arrays
const ar = [1, 2, 3, 4];
console.log(ar);

// () --> Functions
function suma(a, b) {
    return a + b;
}

// {} --> Llaves para objetos, funciones y estructuras de control.
const movil = {
    anchura: 5,
    altura: 10
}

if (true) {
    const constString = "Esto es una string";
}